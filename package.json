{
  "name": "vscode-extension-boilerplate",
  "displayName": "VSCode Extension Boilerplate",
  "description": "A boilerplate for building VSCode extensions with a React-based WebView UI",
  "version": "0.1.0",
  "author": "atman-33",
  "publisher": "atman-dev",
  "icon": "icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/atman-33/vscode-extension-boilerplate"
  },
  "engines": {
    "vscode": "^1.100.0"
  },
  "categories": [],
  "activationEvents": [],
  "main": "dist/extension.js",
  "extensionKind": [
    "workspace",
    "ui"
  ],
  "scripts": {
    "install:all": "npm install && cd webview-ui && npm install",
    "build:ext": "esbuild src/extension.ts --bundle --platform=node --target=node20 --external:vscode --outfile=dist/extension.js",
    "build": "npm run build:ext && npm run build:webview",
    "vscode:prepublish": "npm run build",
    "compile": "npm run build && npm run package",
    "watch": "concurrently \"tsc -w -p ./\" \"npm:watch:webview\"",
    "package": "vsce package",
    "lint": "ultracite check",
    "format": "ultracite fix",
    "check": "ultracite check",
    "test": "vitest run",
    "test:watch": "vitest --watch",
    "test:coverage": "vitest --coverage",
    "prepare": "husky",
    "--- WEBVIEW ---": "--- --- --- --- ---",
    "build:webview": "npm --prefix webview-ui run build",
    "watch:webview": "npm --prefix webview-ui run dev"
  },
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "vscode-extension-boilerplate",
          "title": "VSCode Extension Boilerplate",
          "icon": "./icons/sample.svg"
        }
      ]
    },
    "commands": [
      {
        "command": "vscode-extension-boilerplate.showInformation",
        "title": "Boilerplate: Show Information"
      },
      {
        "command": "vscode-extension-boilerplate.reverseSelection",
        "title": "Boilerplate: Reverse Selection"
      }
    ],
    "menus": {
      "commandPalette": [
        {
          "command": "vscode-extension-boilerplate.showInformation",
          "when": "true"
        },
        {
          "command": "vscode-extension-boilerplate.reverseSelection",
          "when": "editorHasSelection"
        }
      ]
    },
    "views": {
      "vscode-extension-boilerplate": [
        {
          "id": "vscode-extension-boilerplate.simpleView",
          "name": "Simple View",
          "type": "webview",
          "icon": ""
        },
        {
          "id": "vscode-extension-boilerplate.interactiveView",
          "name": "Interactive View",
          "type": "webview",
          "icon": ""
        }
      ]
    }
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.2",
    "@types/node": "^22.0.0",
    "@types/vscode": "^1.100.0",
    "@vitest/coverage-v8": "^4.1.10",
    "@vitest/ui": "^4.1.10",
    "@vscode/vsce": "^3.9.2",
    "concurrently": "^10.0.3",
    "esbuild": "^0.28.1",
    "husky": "^9.1.7",
    "typescript": "^5.9.3",
    "ultracite": "^7.9.2",
    "vitest": "^4.1.10"
  }
}
